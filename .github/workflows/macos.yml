name: macos

on:
  push:

jobs:
  build:

    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: init
      run: |
        git submodule init
        git submodule update
        brew install ncurses jansson
    - name: cmake
      run: LDFLAGS="-L/usr/local/opt/ncurses/lib" CPPFLAGS="-I/usr/local/opt/ncurses/include" cmake -S . -B build
    - name: make
      run: make -C build kek
    - name: store artifact in GitHub
      uses: actions/upload-artifact@v4
      with:
        name: my-artifact
        path: build/kek
        retention-days: 2
